// Stub for Next.js `server-only` import during unit tests.
// Next.js tagger ensures these modules never ship to the client; during
// vitest we just neutralize the import so module eval doesn't fail.
export {};
