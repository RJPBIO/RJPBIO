export const metadata = {
  title: "Informe trimestral · BIO-IGNICIÓN",
  description:
    "Informe imprimible de 90 días con sesiones, ánimo, HRV y escalas validadas (PSS-4, SWEMWBS-7, PHQ-2). Generado localmente desde tu dispositivo.",
  robots: { index: false, follow: false },
};

export default function ReporteLayout({ children }) {
  return children;
}
